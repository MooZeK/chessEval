import {useChessContext} from "../../src/ChessContext";

function Thermometer(){

    const {chessContextProps, } = useChessContext()

    const  chardData = chessContextProps{

    }

    return (<></>)
}

export default Thermometer;